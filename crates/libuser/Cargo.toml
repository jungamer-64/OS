[package]
name = "libuser"
version = "0.1.0"
edition = "2021"
license = "MIT"
license-file = "LICENSE"

[lib]
name = "libuser"
path = "src/lib.rs"

[dependencies]
# No external dependencies - pure no_std library

[package]
name = "shell"
version = "0.1.0"
edition = "2021"

[dependencies]
libuser = { path = "../../libuser" }


[profile.release]
panic = "abort"
lto = true
opt-level = "z"  # Optimize for size
codegen-units = 1

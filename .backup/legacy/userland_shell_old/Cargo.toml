[package]
name = "shell"
version = "0.1.0"
edition = "2021"

[dependencies]
# No external dependencies for now, we want a minimal binary

[profile.release]
panic = "abort"
lto = true
opt-level = "z"  # Optimize for size
codegen-units = 1
